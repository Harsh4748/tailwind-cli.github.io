git init  =Naya Git repository banane ke liye
git status = File ka current status dekhna
git add <Filename>  = Files ko staging area me bhejna only one 
git add .  = for all files
git commit  =Staged changes ko save (snapshot) karna
git log  =Commit history dekhna
git branch = Branch dekhna 
git branch <branchname>  = Branch banana
git branch -d development = delete branch
git push    git push origin development  = Local commits GitHub par bhejna
git config --list = CONFIG check
 --Git user setup--  
git config --global user.name ""
git config --global user.email ""




